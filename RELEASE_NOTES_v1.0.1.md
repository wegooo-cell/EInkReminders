# 墨水屏提醒事项 v1.0.1

这是一个以稳定性、可诊断性和无损升级为重点的维护版本。

## 主要变化

- **同步失败不再只有“失败”**：Mac App 会说明是 IP 地址可能变化、设备不可达、网络超时、提醒事项权限不足、协议版本不一致，还是 NOTE4 帧存储失败，并给出下一步处理建议。
- **失败操作自动重试**：NOTE4 发回的完成或延后操作如果暂时无法写入 Apple 提醒事项，会保存在 Mac 本地，后续同步自动重试。
- **更可靠的双向同步**：修正 NOTE4 同步协议、连续完成项目、24 小时制显示、写接口保护与离线反馈。
- **网络恢复更顺畅**：完善 Wi-Fi 重连、配网页返回设置、恢复出厂以及配网 DNS 响应。
- **刷机保留配置**：浏览器分段写入启动程序、分区表和应用，默认不覆盖保存 Wi-Fi 的 NVS 区域。
- **公开页面更稳定**：网页刷机组件自托管，不再依赖外部 CDN。

## 建议升级顺序

1. 先安装 `EInkReminders-macOS-v1.0.1.zip`。
2. 再通过公开刷机页升级 NOTE4，或使用完整固件 `eink-reminders-note4-v1.0.1.bin`。
3. 打开 Mac App，核对 NOTE4 当前 IP 并执行一次“立即同步”。

> Mac App 与固件最好同时升级到 v1.0.1，以确保同步协议一致。

## 文件校验

下载 `SHA256SUMS-v1.0.1.txt` 后，可在终端中执行：

    shasum -a 256 -c SHA256SUMS-v1.0.1.txt
